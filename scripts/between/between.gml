/// @desc between(value to check,lower bound,upper bound)
/// @arg value to check
/// @arg value to check
/// @arg value to check

if (argument1 < argument2)	return (argument0 >= argument1 and argument0 <= argument2);
else						return (argument0 >= argument2 and argument0 <= argument1);