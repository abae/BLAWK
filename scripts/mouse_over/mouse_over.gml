/// @description mouse_over()
///Returns true if cursor is within object's bounding box
return (mouse_x >= bbox_left and
        mouse_x <= bbox_right and
        mouse_y >= bbox_top and
        mouse_y <= bbox_bottom);
